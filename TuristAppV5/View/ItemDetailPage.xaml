<Page
    x:Name="pageRoot"
    x:Class="TuristAppV5.View.ItemDetailPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TuristAppV5.View"
    xmlns:common="using:TuristAppV5.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bm="using:Bing.Maps"
    mc:Ignorable="d">
	<Page.TopAppBar>
		<AppBar>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<StackPanel Orientation="Horizontal">
					<CommandBar>
						<AppBarButton Icon="Flag" Label="Language"/>
					</CommandBar>
				</StackPanel>
				<StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
					<AppBarButton x:Name="RegisterButton" HorizontalAlignment="Stretch" Icon="Add" Label="Register" VerticalAlignment="Stretch"/>
					<AppBarButton x:Name="LoginButton" HorizontalAlignment="Stretch" Icon="Contact" Label="Login" VerticalAlignment="Stretch"/>
				</StackPanel>
			</Grid>
		</AppBar>
	</Page.TopAppBar>
	<Page.BottomAppBar>
		<AppBar Height="608" Margin="502,0,564,0" ManipulationMode="None">
			<StackPanel>
				<StackPanel Height="87" Width="296">
					<TextBlock TextWrapping="Wrap" Text="      Order" Height="146" HorizontalAlignment="Center" Width="296" VerticalAlignment="Center" FontSize="48"/>
				</StackPanel>
				<StackPanel Height="129" Width="293">
					<TextBlock TextWrapping="Wrap" Text="Date &amp; Time:" FontSize="26.667"/>
					<DatePicker HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
					<TextBlock TextWrapping="Wrap">
						<Run/>
						<LineBreak/>
						<Run/>
					</TextBlock>
					<TimePicker HorizontalAlignment="Center" VerticalAlignment="Center" Height="35" ClockIdentifier="24HourClock" Time="12:30:0"/>
				</StackPanel>
				<StackPanel Height="16" Width="100"/>
				<StackPanel Height="296" Width="301">
					<TextBlock TextWrapping="Wrap" FontSize="26.667" Height="35">
						<Run Text="Name"/>
						<LineBreak/>
						<Run/>
					</TextBlock>
					<TextBox TextWrapping="Wrap" FontSize="26.667" Height="44"/>
					<TextBlock TextWrapping="Wrap" FontSize="26.667" Height="62">
						<Run/>
						<LineBreak/>
						<Run Text="Phone"/>
						<LineBreak/>
						<Run/>
						<LineBreak/>
						<Run/>
					</TextBlock>
					<TextBox TextWrapping="Wrap" FontSize="26.667" Height="41"/>
					<TextBlock TextWrapping="Wrap" Height="59" VerticalAlignment="Bottom">
						<LineBreak/>
						<Run FontSize="26.667"/>
						<LineBreak/>
						<Run FontSize="26.667" Text="E-mail"/>
					</TextBlock>
					<TextBox TextWrapping="Wrap" Text="&#xD;&#xA;"/>
				</StackPanel>
				<StackPanel Height="73" Width="297">
					<AppBarButton x:Name="OrderButton" HorizontalAlignment="Stretch" Icon="Add" Label="Place Order" VerticalAlignment="Stretch" Height="89"/>
				</StackPanel>
			</StackPanel>
		</AppBar>
	</Page.BottomAppBar>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--
            TODO: Content should be placed within the following grid 
                  to show details for the current item
        -->

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{Binding Title}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
            <bm:Map Credentials="AntbJxTZZb2Q4bKMr0xQktLpJsaE1GUU1xQOhYDpLmof3c_88gDrCqDwKE2VR2xG" x:Name="myMap" Margin="39,473,923.429,-582" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>
</Page>
